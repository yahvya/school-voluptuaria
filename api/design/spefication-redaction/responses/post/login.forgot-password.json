{
    "type" : ["object"],
    "properties": {
        "status" : {
            "type": ["object"],
            "properties": {
                "error-message" : {
                    "type": ["string","null"],
                    "description": "Error message or null on success"
                },
                "confirmation-code" : {
                    "type": ["string","null"],
                    "description": "the encrypted confirmation code to send back on confirmation or null on error"
                }
            },
            "required" : ["error-message","confirmation-code"]
        }
    },
    "required" : "status"
}